FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖（包括开发依赖）
RUN npm install

# 安装ffmpeg（用于音频处理）
RUN apk add --no-cache ffmpeg

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3001

# 启动开发服务器
CMD ["npm", "run", "dev"]